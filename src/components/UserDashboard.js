import React from "react";
import { Typography, Box } from "@mui/material";

const UserDashboard = () => (
  <Box>
    {/* <Typography variant="h4">User Dashboard - Limited Access</Typography>
    <p>View appointments and personal info.</p> */}
  </Box>
);

export default UserDashboard;
